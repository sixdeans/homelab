{{ caddy_log_dir }}/*.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 0644 {{ caddy_user }} {{ caddy_group }}
    postrotate
        /bin/systemctl reload caddy > /dev/null 2>&1 || true
    endscript
}
